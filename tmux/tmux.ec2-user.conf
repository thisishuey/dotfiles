bind -T root "C-\\" \
  set prefix None \;\
  set key-table off \;\
  set status off \;\
  if -F "#{pane_in_mode}" "send-keys -X cancel" \;\
  refresh-client -S \;\

bind -T off "C-\\" \
  set -u prefix \;\
  set -u key-table \;\
  set -u status \;\
  refresh-client -S \;\
