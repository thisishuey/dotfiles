bind -T root C-f \
  set prefix None \;\
  set key-table off \;\
  set status off \;\
  if -F "#{pane_in_mode}" "send-keys -X cancel" \;\
  refresh-client -S \;\

bind -T off C-f \
  set -u prefix \;\
  set -u key-table \;\
  set -u status \;\
  refresh-client -S \;\
